# API Keys for LLM access
# The project currently defaults to using the NVIDIA API for Llama 3.3
NVIDIA_API_KEY="your_nvidia_api_key_here"

# (Optional) If you wish to switch back to Groq, set this and modify the LLM instantiation
GROQ_API_KEY="your_groq_api_key_here"
